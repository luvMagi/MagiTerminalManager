"""MagiTerminalManager - GUI generator for Windows Terminal workspaces and appearance.

The long name `MagiTerminalManager` is the product; `magiterm` is the short name used for
every machine-visible identifier (this package, the CLI, the `User.MagiTerm.*` id namespace
in `settings.json`, and the generated `magiterm-theme.ps1`).
"""

__version__ = "0.1.0.dev0"

__all__ = ["__version__"]
